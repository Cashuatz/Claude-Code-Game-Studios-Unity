# Oracle Run: ORUN-2026-04-24-008

- Oracle ID: OR-07 (Negative Oracle)
- Target: 현재 PCG + FlowField 설계에서 "적이 건물 위치를 통과한다" 는 사건이
  발생할 수 없는가 (negative condition: 이 사건이 발생하면 FAIL).
- Input:
  - Negative condition: 적이 Building Cube 가 배치된 위치를 통과 가능해서는 안 됨
  - 검증 방법: (1) 건물 프리팹 Collider 존재 여부, (2) FlowField blocked 등록 경로,
    (3) FixedPathing 장애물 우회 메커니즘
  - 소스: CityscapeAssetBuilder.cs:113~115, td-flowfield-builder.md, td-pathing-fixed.md
- Result: fail (violation)
- Evidence:
  - negative_conditions_checked:
    1. "건물에 Collider 있어 Physics 질의로 통과 차단" → VIOLATED.
       `AssetBuilder.cs:113~115` 에서 Collider 명시적 DestroyImmediate.
       PCG_Building_Cube.prefab: Collider 없음.
    2. "FlowField blocked 배열에 건물 위치 등록됨" → VIOLATED.
       `CityscapeGenerator.GenerateResult` 에 blocked 배열 없음.
       FlowField-PCG 간 데이터 경로 없음.
    3. "FixedPathing 이 건물 위치를 우회하는 웨이포인트를 가짐" → NOT TESTED.
       FixedPathing 은 레벨 JSON 에서 웨이포인트를 로드. PCG 건물 배치와 무관.
       웨이포인트가 건물 위치를 통과하지 않도록 보장하는 명세 없음.
  - violations_detail: 조건 1, 2 모두 위반. 조건 3 미테스트.
- Limitations observed:
  - OR-07 은 현재 "코드/명세 정적 분석" 기반. 실제 런타임 충돌 여부는
    OR-P-001(Play Mode) 로 동적 확인 필요 — 단, Escalation #5 해소 후 진행 가능.
  - FixedPathing(Q2-A) 은 웨이포인트 JSON 이 올바르게 설계되면 건물 통과를
    우연히 피할 수 있음. 그러나 이는 "명시적 장애물 등록" 이 아닌
    "우연한 회피" 이므로 negative condition 충족으로 볼 수 없음.
