# Oracle Run: ORUN-2026-04-24-005

- Oracle ID: OR-08 (Human Oracle) — 2차 판정 루프
- Target: ORUN-2026-04-24-003 에서 REJECTED 된 "시가지 PCG 결과가 너무 균일함"
  이슈가 2회차 구현(블록 타입 3분화 + 높이 power curve + yaw jitter) 으로
  해소되었는지.
- Input:
  - Auto Test 3시드 실행 로그 (ORUN-2026-04-24-004 동일 소스)
  - 시드별 블록 타입 분포 / 건물·화단 수 / 풀 수 변동:
    - seed=1    → O2/M8/D6, bld=26, plan=7,  grass=486
    - seed=42   → O5/M7/D4, bld=22, plan=5,  grass=467
    - seed=9999 → O3/M9/D4, bld=18, plan=9,  grass=702
  - 대기 중 입력: 사용자 씬뷰 시각 판정
- Result: uncertain (수치상 2차 통과 / 시각 판정 대기)
- Evidence:
  - 수치 측면 지지 근거:
    - 블록 타입 O/M/D 비율이 3 시드에서 모두 다름 (2/8/6, 5/7/4, 3/9/4)
      → 의도된 분포 분기가 실제로 동작.
    - 건물 개수 편차 18~26 (최소 lots=27 공통일 때 18 vs 22 vs 26 로 건물 확률 편차 확인)
    - 화단 비율: seed=9999 가 화단 9 / 건물 18 = 1:2 인 반면, seed=1 은 화단 7 / 건물 26 ≒ 1:3.7
      → 블록 밀도 분포가 시드별로 달라짐.
  - 수치 측면 반대 근거:
    - 블록 수(16) / 도로(10) / 페인트(10) / 연석(64) 은 모든 시드 공통 — 인프라 계층은
      시드 무관. 이는 의도된 동작(그리드 고정) 이나, 블록 경계선의 격자 규칙성이 여전히
      강할 수 있음. → 시각 판정 시 "격자감" 이 여전히 느껴지면 추가 개선 필요
      (블록 단위 스킵, 도로 폭 jitter 등).
- Limitations observed:
  - OR-08 은 본질적으로 사용자 판정. Claude 는 수치 근거만 제공 가능.
  - "다양성 충분" 의 정량 기준 부재. 후속 proposal 여지:
    `OR-P-PCG-variance` 프로젝트 오라클 신설 (예: blockDensity 엔트로피 ≥ 0.8,
    buildingHeight 표준편차 ≥ min·max 간격의 20% 등).
  - 씬뷰 스크린샷을 MCP `manage_camera` 로 자동 수집해 Claude 가 시각 판정까지
    보조하는 경로도 가능 — 다음 루프에서 평가.
