# Oracle Run: ORUN-2026-04-24-007

- **Oracle ID**: OR-08 (Human Oracle) + OR-05 (Comparison Oracle) 이중 적용
- **Target**: `ProtoEnv_BushBillboard.shader` Rev B — 방향성 조명이
  비교군 Lit 구 대비 "시각적으로 납득 가능" 한 수준인가.
- **Context**: OBS-2026-04-24-001 에서 Rev A (half-Lambert, ambient 0.22)
  가 명암 대비 ~1.7× 수준이라 방향성 조명이 약하다는 사용자 지적 발생.
  Rev B 는 wrap-Lambert(`_WrapAmount=0.15`) + ambient 를 (0.08, 0.10, 0.07)
  로 축소.

## Input
- 씬: `Assets/Proto/Scenes/BushDemo.unity`
- Play 모드 3초 후 스크린샷: `Assets/Screenshots/bush_demo_v3_lambert.png`
- 참조(Rev A): `Assets/Screenshots/bush_demo_v2_quadsize_fixed.png`
- 동일 씬에 URP Lit 구(reference) 포함.

## Result
**PASS** — 수치·정성 근거 양쪽 모두 긍정 (사용자 최종 판정 대기 중).

## Evidence

### 정량 근거 (Rev B 조명 수식)
```
wrapLambert = saturate((NoL + 0.15) / 1.15);   // NoL=1 → 1.0 / NoL=-1 → 0.0
directLit   = mainLight.color * wrapLambert;
_AmbientColor = (0.08, 0.10, 0.07)
col          = albedo * (directLit + _AmbientColor) + albedo * back
```

실질 명암비 (mainLight.color=(1,1,1), back=0 가정):
- 직사광 측: `1.0 + ~0.09 ≈ 1.09 × albedo`
- 그림자 측: `0.0 + ~0.09 ≈ 0.09 × albedo`
- **대비비 ≈ 12×** (Rev A 의 ~1.7× 대비 7× 향상)
- wrap=0.15 덕분에 N·L=0 지점에서도 완전 0 이 아닌 ~0.13 → 잎 부드러움 유지.

### 정성 근거 (OR-05 시각 비교)
- Rev B 스크린샷 (`bush_demo_v3_lambert.png`):
  - 부쉬 상단(태양 방향) 밝은 녹색 피크.
  - 부쉬 하단 뚜렷한 음영 (거의 검은 녹색).
  - 명암 경계가 구의 방향성을 반영 (원본 노멀 보존 확인).
- 동일 프레임 Lit 구(reference) 와 명암 분포 방향 일치.
- Rev A 대비 "평면적" → "입체적" 전환 명확.

### 퍼-쿼드 빌보드 유지
- 카메라 궤도 회전 중(스크린샷 시점) 에도 모든 쿼드 카드가 카메라 향해 정면.
- 노멀은 원본 구 외향 방향 유지 → 라이팅만 구처럼, 실루엣은 잎 카드.

## Limitations
- OR-08 최종 판정은 사용자 몫. Claude 는 수치·정성 근거까지만 제공.
- "납득 가능" 의 프로젝트 차원 정량 기준은 현재 부재. 추후 proposal:
  `OR-P-LIGHT-01` (대비비 최소 4× 권장) 신설 검토.
- 백라이트(역광 실루엣) 는 본 스크린샷에서 시각 확인 불가 — 태양이 카메라
  반대편에 놓이는 구도가 아님. 별도 각도 스크린샷 필요 (TBD).
- `mainLight.color` 가 URP 에서 실제로 세팅됨을 런타임 로그로 확인하지 않음.
  시각 결과가 예측과 일치하므로 간접 확인으로 인정 (엄격 검증이 필요하면
  Frame Debugger 로 별도 확인).

## Next Action
- 사용자가 `bush_demo_v2_quadsize_fixed.png` 와 `bush_demo_v3_lambert.png`
  를 육안 비교하여 "명암 충분" 최종 판정.
- 반대 각도 백라이트 씬 테스트는 TBD (재방문 조건: 실 레벨에서 역광
  상황이 발생하거나, 사용자 요청 시).
