# Case Log: CL-2026-04-24-001

## Metadata
- Case ID: CL-2026-04-24-001
- Date: 2026-04-24
- Session: proto-td-pcg-cityscape-initial
- Operator: Claude

## Loop Goal
TD 맵을 위한 시가지 그레이박싱 PCG 모듈(`Proto.Stage.PCG.CityscapeGenerator`) 의
초기 구현이 (1) 컴파일 에러 없이 빌드되고, (2) 컨벤션 하드룰 HR-1/3/8/10 을
위반하지 않으며, (3) 프리팹 교체를 통한 아트 전환이 가능한지 검증한다.

## Input Summary
- 디자인 컨벤션: `design/proto-modules/_conventions.md` (HR-1 ~ HR-11)
- 기존 모듈 문서: `design/proto-modules/pcg-city-layout.md` (스코프: StageGraph 노드 그래프)
- 사용자 요구사항 (이 세션):
  - 시가지 PCG + 6종 프리미티브(쿼드 2 / 큐브 3 / 스피어 1) 그레이박싱
  - 실제 쿼드·큐브 직접 생성이 아니라 **프리팹 교체 가능한 구조**로 작성
  - 에디터 윈도우(Unity UI Toolkit)에서 Generate/Clear/AutoTest 버튼과
    메트릭·로그 표시 (메트릭은 반드시 복사 가능해야 함)
- 추가 요구 (OR-08 Human Oracle 1회차 피드백):
  - 생성 결과가 "너무 균일함" → 비대칭성 주입 필요

## Observed Signals
- Logs: Unity console `read_console(types=error)` → 0건 (시가지 PCG 코드 기인 에러 없음)
  - 기존 `Assets/Proto/VFX/Shared/Shaders/ProtoFx_Scanline.shader` 의 셰이더 에러 2건은 이 모듈과 무관
- Artifacts: 새로 생성된 소스 파일 목록
  - `Assets/Proto/Runtime/Stage/PCG/CityscapeProfile.cs` (SO, 수치)
  - `Assets/Proto/Runtime/Stage/PCG/CityscapeMetrics.cs` (결과 struct + ToReport)
  - `Assets/Proto/Runtime/Stage/PCG/CityscapeAssetSet.cs` (SO, 프리팹 6슬롯)
  - `Assets/Proto/Runtime/Stage/PCG/CityscapeGenerator.cs` (Block Subdivision + 프리팹 Instantiate)
  - `Assets/Proto/Editor/Stage/PCG/CityscapeAssetBuilder.cs` (Editor 유틸, 기본 프리팹/머티리얼 생성)
  - `Assets/Proto/Editor/Stage/PCG/CityscapeWindow.cs` (UI Toolkit EditorWindow)
- Execution:
  - 1회차 Generate (Label 기반 메트릭, 프리미티브 직접 생성): 사용자 Play 확인 → 동작 OK, 단 "균일함" 지적
  - 2회차 미실행 — 본 Case Log 가 커버하는 빌드는 Instantiate 기반 재작성 이후 상태

## Applied Oracles
| Oracle ID | 판정 결과 | 근거 |
|-----------|----------|------|
| OR-P-004  | pass     | Unity `read_console(types=error)` 0건 — 본 모듈 5 파일 컴파일 성공. ORUN-2026-04-24-001 참조. |
| OR-01     | pass     | HR-1/3/8/10 에 대해 소스 코드 정적 검사 통과. ORUN-2026-04-24-002 참조. |
| OR-08     | rejected | 사용자(Human Oracle) 1회차 실행 피드백: "너무 균일함". 개선 방향(블록 타입 3분화 + 건물 높이 power curve + 회전 jitter) 으로 코드 재작성. ORUN-2026-04-24-003 참조. |
| OR-06     | uncertain | 결정론 메타모픽 관계 (`Generate(seed=X)` 재실행 시 동일 결과) 검증 미실행. 다음 루프에서 Unity MCP `execute_code` 로 확인 예정. |

## Failure Classification
| Failure ID | 분류명 | 심각도 | upstream/downstream |
|------------|--------|--------|---------------------|
| FT-08      | Validation Shortfall — 사용자 피드백 전까지 시각적 다양성 판정 누락 | minor | isolated |

## Decision
uncertain

컴파일 (OR-P-004) 과 하드룰 준수 (OR-01) 는 PASS. 사용자 시각 판정 (OR-08)
은 1회차 구현이 REJECTED 됨 — 2회차에서 비대칭성(블록 타입 분포, 높이 power,
회전 jitter) 을 주입해 프리팹 기반으로 재작성 완료. 재실행 후 OR-08 재판정
및 OR-06 결정론 검증 필요.

## Next Action
사용자에게 에디터 윈도우에서 "Build Default Asset Set → Auto Test (seeds 1, 42, 9999)"
실행을 요청하고, Debug.Log 로 남긴 메트릭 3회차 결과를 `read_console` 로 수집해
(a) OR-06 결정론 (같은 시드 재생성 시 동일 GO 수) (b) OR-08 다양성 재판정을 수행한다.

## Related Proposal
없음 (현재까지는 기존 HR 체계 내 작업. 프로토 전용 PCG 오라클 `OR-P-PCG-determinism`
신설 가치가 있으면 후속 proposal 로 제안).

## Lessons
1. Proto 모드에서도 VKL Oracle ID 부착은 "선택" 이 아님. 사용자 질문
   ("VKL 체크도 하고 있는건가?") 이 들어오기 전에 기본으로 runtime 로그를 남겨야 함.
2. PCG 그레이박싱은 **프리팹 교체 인터페이스 (AssetSet SO)** 를 1회차부터 두는 게
   옳다. 기본 구현이 primitive-직접생성으로 간 것은 스코프 누락 — 사용자 피드백 한 번에
   재작성이 필요했다.
3. 에디터 윈도우의 "복사 가능한 메트릭" 은 단순 Label 로는 안 되고
   `TextField(readOnly=true, multiline=true)` + `Debug.Log` 병행이 필요.
   후자는 Claude 가 `mcp__unityMCP__read_console` 로 직접 읽는 데 필수.
