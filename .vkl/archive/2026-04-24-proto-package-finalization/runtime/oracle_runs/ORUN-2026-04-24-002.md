# Oracle Run: ORUN-2026-04-24-002

- Oracle ID: OR-01 (Spec Oracle)
- Target: 신규 PCG 모듈이 `design/proto-modules/_conventions.md` 의 Hard Rules
  (HR-1, HR-3, HR-8, HR-10) 를 위반하지 않는다.
- Input:
  - 명세 문서: `design/proto-modules/_conventions.md` 의 HR-1 ~ HR-11 섹션
  - 구현 파일 (정적 소스 코드 검사):
    - `CityscapeProfile.cs` / `CityscapeMetrics.cs` / `CityscapeAssetSet.cs`
    - `CityscapeGenerator.cs` / `CityscapeAssetBuilder.cs` / `CityscapeWindow.cs`
  - 관찰 방법: Grep 으로 금지 키워드(`Rigidbody`, `Time.timeScale`, `SceneManager.LoadScene`,
    `UIDocument`, `Packages/com.coplaydev.unity-mcp/`) 부재 확인 + 소스 로직 독해
- Result: pass
- Evidence:
  - HR-1 (Rigidbody 금지):
    - 생성된 프리팹은 `GameObject.CreatePrimitive` 결과에서 `Collider` 를 `Object.DestroyImmediate` 로 제거.
    - `Rigidbody` 컴포넌트를 추가하는 코드 부재 (정적 검사).
  - HR-3 (PCG 시드 결정론):
    - `CityscapeGenerator.Generate` 는 `new System.Random(seed)` 기반.
    - `UnityEngine.Random` 은 `CityscapeWindow.RandomizeSeed` 에서만 사용(시드 값 선택 용도). 생성 경로 안에서는 사용 안 함.
  - HR-8 (데이터-코드 분리):
    - 수치는 모두 `CityscapeProfile` SO(citySize, blockSize, roadWidth, buildingHeightMin/Max, grassDensityPerSqm 등).
    - 코드 상수는 `BuildingHeightPower = 2.5f`, `BuildingYawJitterDeg = 4f` 2개로 분포 형태 결정용.
      추후 Profile SO 에 승격 가능.
    - 하드코드된 색상(ColorRoad 등) 은 기본 그레이박싱 머티리얼 초기값으로만 사용되며
      실 머티리얼은 `Assets/Proto/Materials/PCG/` 의 에셋이 소스 오브 트루스.
  - HR-10 (Assets/Proto/ 하위 전용):
    - 모든 생성 경로 상수는 `Assets/Proto/` 로 시작 (`Assets/Proto/Data/Config/`,
      `Assets/Proto/Materials/PCG/`, `Assets/Proto/Prefabs/Stage/PCG/`).
    - `Packages/com.coplaydev.unity-mcp/` 쓰기 부재.
- Limitations observed:
  - 정적 소스 검사는 **런타임 Rigidbody 동적 추가 (AddComponent<Rigidbody>)** 는 보지 못함.
    현재 코드에는 해당 호출이 없지만, 미래 리팩토링에서 들어올 위험 있음 — `/proto-lint`
    에디터 validator 가 나오면 재검증.
  - HR-8 "Formula 필드" 는 Ability 를 위한 것이라 PCG 분포형 상수(Power 2.5 등) 에는
    직접 적용되지 않음. 명세 gap 여지 있음 — OR-01 confidence: medium.
  - HR-2 (단일 씬), HR-11 (uGUI) 등은 본 오라클 범위 외. Editor 윈도우는 UI Toolkit 허용 규칙 따름.
