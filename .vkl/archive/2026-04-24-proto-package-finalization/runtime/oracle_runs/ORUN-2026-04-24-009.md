# Oracle Run: ORUN-2026-04-24-009

- Oracle ID: OR-04 (Observability Oracle)
- Target: 건물 lot 위치가 FlowField blocked 배열로 흐르는 데이터 경로가
  런타임에서 추적 가능한가.
- Input:
  - CityscapeGenerator.cs 의 BuildLot() → SpawnCube() 흐름
  - CityscapeMetrics struct 필드 목록
  - FlowFieldBuilder.Build() 시그니처
  - td-pathing-flowfield.md 내 빌드 호출 시점 기술
- Result: insufficient
- Evidence:
  - required_info (건물→blocked 흐름 추적에 필요한 정보):
    1. 건물 lot rect 좌표 (x, z, width, height) → Generator 내부에만 있고 외부 노출 없음
    2. FlowField 그리드 원점/셀 크기와 PCG 그리드 정합 정보 → 명세 없음
    3. blocked 배열이 채워지는 시점 (레벨 로드? 씬 생성? 에디터 타임?) → 명세 없음
    4. blocked 배열 내용 확인 가능한 로그/gizmo → 설계 없음
  - coverage: 0% (네 항목 모두 부재)
  - recommendation:
    - Option A 선택 시: GenerateResult 에 blocked 배열 추가 + Debug.Log 덤프 구현
    - Option B 선택 시: Physics.OverlapBox 질의 후 hit 결과 로그 구현
    - Option C 선택 시: CityscapeObstacleMap SO Inspector 확인 가능
- Limitations observed:
  - OR-04 는 "관측 인프라가 있는가" 판정. "데이터가 올바른가"는 별개.
    관측 인프라 구축 전까지 OR-06/OR-07 의 "property 테스트" 실행 불가.
