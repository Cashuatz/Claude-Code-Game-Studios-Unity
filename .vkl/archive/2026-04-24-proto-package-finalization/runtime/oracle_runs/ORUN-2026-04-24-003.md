# Oracle Run: ORUN-2026-04-24-003

- Oracle ID: OR-08 (Human Oracle)
- Target: 1회차 `CityscapeGenerator` 출력의 **시각적 다양성(variance)** 이
  시가지 그레이박싱 용도로 충분한지.
- Input:
  - 사용자 1회차 실행 (Block Subdivision 기본 구현, splitChance=0.65 고정,
    건물 높이 선형 Lerp, 건물 회전 없음, 블록 단위 스타일 분기 없음)
  - 사용자 응답 원문: "일단 정상동작. 단, 너무 균일함."
  - 이전 컨텍스트: 그레이박싱 단계이므로 과한 다양성은 불필요하나,
    블록 간/lot 간 최소한의 대비는 필요 (스카이라인 실루엣 및 공간 밀도 변화)
- Result: uncertain (1회차 REJECTED → 2회차 재판정 대기)
- Evidence:
  - 1회차 판정: `rejected`
    - 사용자 직접 판정: "너무 균일함"
    - 직관적 원인:
      1. 모든 lot 이 같은 `splitChance` + 같은 `buildingProbability` 사용 → 블록 간 밀도 차이 없음
      2. 건물 높이 분포가 `Lerp(min, max, uniform)` → 고층과 저층이 균등 분포 → 스카이라인 평탄
      3. 건물 회전이 정확히 축정렬(0°) → 격자 느낌 강함
  - 2회차 개선 코드 (현재 반영):
    - 블록 타입 3종 (`BlockDensity.Open / Mixed / Dense`) 시드 기반 배분 (20/50/30)
    - Open: depth=0, buildingProb=0.30, heightScale=0.6 (광장·공원)
    - Dense: depth+1, buildingProb+0.15, heightScale=1.4 (고밀도)
    - 건물 높이 분포에 `Pow(t, 2.5)` 적용 → 낮은 건물 다수, 고층 드물게
    - 건물 yaw jitter ±4°
  - 2회차 재판정 대기 (사용자가 `Auto Test (seeds 1, 42, 9999)` 실행 후 결과 전달 필요)
- Limitations observed:
  - OR-08 은 주관적 — 같은 "다양성" 용어가 스카이라인 실루엣 다양성인지,
    블록 단위 테마 다양성인지, lot 단위 형태 다양성인지 사용자와 정확한 기준 합의 부재 (FT-01 여지).
  - 자동화된 variance metric (예: 건물 높이 표준편차, 블록 밀도 분산) 을 제공하지 않아
    Claude 가 사전 자체 판정할 수 없음 → OR-06 Metamorphic 로 보완 (예: "Dense 블록이
    Open 블록보다 buildingCount 가 명확히 크다" 불변식) 가능.
  - 본 판정은 시각적 품질로서, OR-P-001 Play Mode 스크린샷 비교 보조 필요.
