# Oracle Run: ORUN-2026-04-24-004

- Oracle ID: OR-06 (Metamorphic Oracle)
- Target: `CityscapeGenerator.Generate(seed=X, ...)` 를 동일 시드로 재호출했을 때
  생성물 수량 메트릭(blocks, O/M/D 분포, lots, road, paint, curb, building, planter,
  grass, totalGO) 이 완전히 동일하게 재현된다. (결정론 불변식)
- Input:
  - 관찰 소스: Unity `Debug.Log` → MCP `read_console(filter_text="Cityscape")` 로 수집
  - Auto Test 실행 결과 (seed 1, 42, 9999 순차 실행 + seed=9999 1회 추가 실행)
  - 비교 대상 행:
    - `[Cityscape] seed=9999 ms=30.6 blocks=16(O3/M9/D4) lots=27 GO=813 road=10 paint=10 curb=64 bld=18 plan=9 grass=702`
    - `[Cityscape] seed=9999 ms=26.5 blocks=16(O3/M9/D4) lots=27 GO=813 road=10 paint=10 curb=64 bld=18 plan=9 grass=702`
  - Metamorphic relation: MR = "same seed ⇒ same counts (ms 제외 모든 필드)"
- Result: pass (RELATION_HOLDS)
- Evidence:
  - 모든 카운트 필드가 bit-identical 일치:
    blocks=16, O=3, M=9, D=4, lots=27, GO=813, road=10, paint=10, curb=64,
    bld=18, plan=9, grass=702.
  - 실행 시간(ms) 만 30.6 → 26.5 로 상이 — 이는 관계 대상에서 제외된 필드.
  - 다른 두 시드(1, 42) 와 비교 시 카운트 필드가 모두 서로 다름 → relation 이
    "trivially always true" 가 아니라 시드에 실제로 의존함을 검증.
- Limitations observed:
  - 메트릭 수량만 비교 — 실제 `Transform.position`/`localScale`/yaw jitter 까지 bit-identical
    인지는 본 오라클로 판정 불가. `execute_code` 로 각 자식 Transform 덤프를 비교해야
    완전 검증 가능 → 후속 ORUN 에서 필요 시 추가.
  - 프레임 순서·스케줄링에 영향을 주는 Editor 상태(AssetDatabase 캐시, 도메인 리로드
    직후 여부) 에 따른 미묘한 차이는 본 오라클 커버리지 밖.
