# Hypothesis: HYP-2026-04-24-002

- Note: 본 기록은 원래 HYP-2026-04-24-001 로 작성됐으나, origin/Proto 병합 시
  같은 ID가 Phase 0 공통 캐릭터 프리팹 전략 HYP 에 먼저 할당되어 있어 HYP-002 로
  재등록. 내용은 그대로.
- Hypothesis: FlowField blocked 배열은 CityscapeGenerator 가 building lot rect 좌표를
  직접 그리드 셀 인덱스로 변환하여 반환하는 방식(Option A)으로 구현하는 것이 가장
  HR-1 친화적이고 데이터 흐름이 단순하다.
- Confidence: medium
- Evidence for:
  - Generator 는 SubdivideAndBuildLots 에서 building lot rect (x, z, w, h) 를 이미
    계산하고 있다. 이를 blocked 배열로 변환하는 것은 추가 연산 최소.
  - HR-1 (Rigidbody 금지): Option A 는 Physics 관여 없음. 완전 준수.
  - HR-3 (결정론): 시드 기반이므로 동일 시드 → 동일 blocked 배열.
- Evidence against:
  - FlowField 그리드 (셀 크기 1m/cell, td-flowfield-builder.md 기본값) 와
    PCG 그리드 (blockSize 30m, roadWidth 5~20m) 의 원점·해상도 정합이 필요.
    정합 실패 시 blocked 배열이 시각 오브젝트 위치와 어긋남.
  - "프리팹 교체"(AssetSet) 시 건물 footprint 가 달라질 수 있는데,
    Generator 는 lot rect 기준으로만 blocked 를 계산함 → 실 에셋 footprint 와
    blocked 범위 불일치 가능성.
- Test plan:
  - Option A 구현 후 blocked 배열 덤프 로그 추가.
  - 동일 시드로 Generate 2회 → blocked 배열 bit-exact 동일 (OR-06, MR 재사용).
  - blocked 배열에서 True 인 셀 위치에 시각적 마커 배치 → 건물 위치와 오버랩 확인.
- Status: tentative
- Supersedes: 없음
- Related: CL-2026-04-24-014
